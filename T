@pytest.fixture
def mock_service(
    mock_conveying_repo,
    mock_individual_repo,
    mock_entity_repo,
    mock_email_repo,
    mock_phone_repo,
):
    service = ConveyingPartyService()

    service.conveying_party_repo = mock_conveying_repo
    service.conveying_party_repository = mock_conveying_repo

    service.individual_repo = mock_individual_repo
    service.individual_repository = mock_individual_repo

    service.entity_repo = mock_entity_repo
    service.entity_repository = mock_entity_repo

    service.individual_email_info_repo = mock_email_repo
    service.individual_email_info_repository = mock_email_repo

    service.individual_phone_info_repo = mock_phone_repo
    service.individual_phone_info_repository = mock_phone_repo

    return service